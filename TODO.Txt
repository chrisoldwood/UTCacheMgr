- Folder checking (remote paths cause a problem).

- Restore log, cleanup up invalid records.

- Better icons for properties dialog.

- New class CIconCtrl (Replace code in file properties dialog).

- Show actual cache file name.

- Find file by (partial) name.

- Restore dialog, add date and sorting options.

- Show cache directory size (files/MB) in status bar.
